/**
 * Company business logic — Sprint 2+ (not implemented).
 *
 * @example
 * class CompanyService {
 *   constructor(private readonly companyRepository: CompanyRepository) {}
 *   async switchTenant(companyId: string) { ... }
 * }
 */
export class CompanyService {
  // Sprint 2+: orchestrate company operations via CompanyRepository
}
